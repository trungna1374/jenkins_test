version: '3.7'
volumes:
  pgdata_development: {}
  logs_development: {}
services:
  app:
    platform: linux/x86_64
    container_name: jenkins-test-spring-boot
    image: jenkins-test-spring-boot
    build: ./
    ports:
      - 3030:3030

